% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lockfile-api.R
\name{lockfile}
\alias{lockfile}
\title{Create a lockfile}
\usage{
lockfile(from = NULL, to = NULL, project = NULL)
}
\arguments{
\item{from}{The source from which the lockfile should be created. This can be:
\itemize{
\item \code{NULL} (the default), in which case the lockfile is created from the
state of the active project's library;
\item The path to a Posit Connect \code{manifest.json} file, or a manifest that
has already been read in as an \R list, in which case the manifest is
converted into a lockfile.
}

The set of supported sources may be expanded in future releases of
\code{renv}.}

\item{to}{The path to a lockfile to be written. When \code{NULL} (the default), the
lockfile is returned as an \R object and not written to disk; otherwise,
the lockfile is written to \code{to} and returned invisibly.}

\item{project}{The project directory. If \code{NULL}, then the active project will
be used. If no project is currently active, then the current working
directory is used instead.}
}
\value{
An \code{renv} lockfile, as an \R object of class \code{renv_lockfile}. When \code{to}
is supplied, the lockfile is returned invisibly.
}
\description{
Create an \code{renv} lockfile from a variety of sources.
}
\details{
\code{lockfile()} provides a generic entry point for producing an \code{renv}
lockfile. The kind of lockfile produced depends on the value supplied as
\code{from}; for example, given the path to a Posit Connect \code{manifest.json}
file, \code{lockfile()} will convert that manifest into a lockfile.

By default the lockfile is returned as an \R object. Supply \code{to} to also
write it to disk, after which \code{\link[=restore]{restore()}} can be used to restore the
associated project library.
}
\examples{

\dontrun{

# create a lockfile from a Posit Connect 'manifest.json' file
lock <- lockfile(from = "manifest.json")

# convert a 'manifest.json' file and write 'renv.lock' in a single call
lockfile(from = "manifest.json", to = "renv.lock")

}
}
\seealso{
\code{\link{lockfiles}}, for a description of the structure of an
\code{renv} lockfile.
}
