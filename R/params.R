#' @param renv The name of a virtual environment. If `NULL`, then the active
#'   virtual environment (if any) will be used.
#' @param config The virtual environment's configuration. See [renv_config()]
#'   for more details.
#' @param project The project directory. If `NULL`, then the project associated
#'   with the active virtual environment (if any) will be used.
#' @param manifest A manifest, describing the state of a virtual environment.
#'   Use [renv_restore()] to restore a virtual environment from a manifest.
#' @param confirm Boolean; prompt the user before taking any action?
#'
#' @name renv-params
NULL
